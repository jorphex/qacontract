# Copy to scripts/wrangler.toml locally.
# Do not commit scripts/wrangler.toml — account_id and routes are injected at deploy time.

name = "kingofthehill-api"
main = "cloudflare_worker.js"
compatibility_date = "2026-06-26"

[vars]
CHAIN_ID = "8453"